#include <stdio.h>
#include<string.h>
void main()
{
 char ch;
   char password [100];
   int i=0;
   printf("enter any password ");
   while((ch=getch())!=13)
{
    password[i]=ch;
    i=i+1;
    printf(" * ");
    
}
password [i]='\0';
printf("entered the password is %s",password);
    
}
